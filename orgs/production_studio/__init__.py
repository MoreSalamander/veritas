"""Production Studio — a media-production organization whose verification model is
CONFORMANCE THROUGH THE CHAIN plus media integrity.

A production is trustworthy not because it is good (goodness has no oracle — that is the human
tier) but because it is *consistent*: every shot traces to a script beat, every beat is covered,
and nothing appears downstream that was not declared upstream. That referential integrity — the
research org's grounding model repointed at production — is what makes "consistent" a fact a
machine can prove. This package is the structural spine: concept -> script -> storyboard as typed
artifacts, gated on completeness, grounding, and coverage. Asset generation (wrapping the
generative engines) and the integrity/format gates come in later phases.
"""
