"""Empirical Lab — an organization whose verification model is REPRODUCIBILITY.

A hypothesis is a claim about the world. It is trustworthy here only if a re-runnable experiment
SUPPORTS it: the experiment runs, produces the predicted measurement, gives the same answer when
run again, and the measured result actually satisfies the hypothesis's stated prediction. The data
decides — never the model's assertion. A claim the experiment refutes is rejected (and the
refutation is remembered); a claim whose experiment won't reproduce is rejected. This is the
software org's execute-and-check pointed at empirical claims, which makes it a genuinely different
"how do we know it's trustworthy" — hence a peer org, not a role. Seeded by the project's bench/.
"""
