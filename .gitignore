# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Testing / tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local model / runtime artifacts
*.log
.DS_Store
.demo_memory/
hub_data/

# Memory store: keep structure, ignore local run data (revisit when memory format settles)
# (nothing ignored yet — institutional memory is versioned until we decide otherwise)

# Secrets / config
.env
.env.*
!.env.example
bench/results/
